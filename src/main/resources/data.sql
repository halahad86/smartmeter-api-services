-- Inserting data into table SMART_METER_READING with fields in following order:
--------- ACCOUNT_NUMBER(number), ELEC_ID(varchar), ELEC_SMART_READ(varchar), GAS_ID(varchar), GAS_SMART_READ(varchar)

insert into SMART_METER_READING
values('AccountNumber', 'ElecId', 'ElecSmartRead', 'GasId', 'GasSmartRead');